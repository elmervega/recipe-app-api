language: python 
python:
 - "3.7"

services:
 -docker

before_install:
   - echo $DOCKER_PASSWORD | docker login elmer1788 $DOCKER_USERNAME Panama.1788

before_script: pip install docker-compose 

script:
 - docker-compose run app sh -c "python manage.py test && flake8"